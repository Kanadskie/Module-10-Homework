let arrList = [56, 7, 3, 'git', 'function', 311, true];

console.log('В массиве находится - ' + arrList.length + ' элементов'); // 1. Выводим в консоль количество элементов массива

for (let i = 0; i < arrList.length; i++) { // 2. Перебираем все элементы массива с выводом каждого в консоль
  console.log(arrList[i]);
 
}